# simple-repo-laravel-angular

You can use Homestead to run.

After cloning:

    $ composer install

    $ php artisan vendor:publish
    
In development mode run:
    
    $ php artisan migrate:refresh --seed
